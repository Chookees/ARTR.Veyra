# Security

ARTR Veyra defaults toward fail-closed admin authentication when enabled, hashed API keys, secret resolution without logging secret values, transform allowlisting, request size limits, and optional forwarded-header trust configuration.

Report vulnerabilities via GitHub private vulnerability reporting ([SECURITY.md](../SECURITY.md)).

See [STRIDE threat model](threat-model-stride.md).
